node_modules
dist
*.local
.DS_Store

# Internal planning notes (kept out of the public repo)
tasks/

# Generated data — rebuilt from Sanity by `npm run fetch:events`. CI regenerates both on
# every deploy (prebuild); `predev` generates them locally on first run if missing.
public/feeds/
public/events.json
